<!DOCTYPE html>
<html>
  <head>
    <title>Job Listing</title>
    <%= csrf_meta_tags%>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'%>
    <%= javascripts_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid">
      <%= render "common/navabar" %>
      <%= render "common/flashes" %>
      <div class="row">
        <div class="col-md-2">
          sidebar
        </div>
        <div class="col-md-10">
          <%= yield %>
        </div>
      </div>
      <%= render "common/footer" %>
    </div>
  </body>
</html>
