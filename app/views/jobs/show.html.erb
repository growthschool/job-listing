<h1> <%= @job.title %> </h1>

<p>
  <%= simple_format(@job.description) %>
  <%= @job.wage_upper_bound %>
  <%= @job.wage_lower_bound %>
  <%= simple_format(@job.contact_email) %>

  <%= link_to("Apply and Upload your Resume", new_job_resume_path(@job), class: "btn btn-primary pull-right") %>

</p>
