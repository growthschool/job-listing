<h2><%= @job.title %></h2>
<div class="col-md-4 col-md-offset-4">
	<hr>
	<%= simple_format(@job.description) %>
	<%= simple_format("#{@job.wage_lower_bound}") %>
	<% if current_user %>

	
<!-- 		<%= link_to "上传简历", new_job_resume_path(@job), class: "btn btn-primary" %>
 -->

		<!-- test code -->
		<%= simple_form_for @resume, url: job_resumes_path(@job) do |f| %>
			<%= f.input :attachment %>
			<%= f.submit "提交", class: "btn btn-primary" %>
		<% end %>


	<% end %>
	
</div>