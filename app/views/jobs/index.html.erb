<div class="pull-right">
  <%= link_to("Add a job",new_job_path,:class => "btn btn-default") %>
</div>
<table class="table table-boldered">
  <% @jobs.each do |job| %>
  <tr>
    <td>
      <%= link_to(job.title, job_path(job)) %>
    </td>
    <td>
      <%= link_to(job.wage_upper_bound,job_path(job)) %>
    </td>
    <td>
      <%= link_to(job.wage_lower_bound,job_path(job)) %>
    </td>
    <td>
      <%= link_to(job.contact_email,job_path(job)) %>
    </td>
    <td>
      <%= link_to("Edit",edit_job_path(job)) %>
      <%= link_to("Destroy",job_path(job),:method => :delete, :data => {:confirm =>"are you sure?"}) %>
    </td>
    <td>
      <%= job.created_at %>
    </td>
  </tr>
  <% end %>
</table>
