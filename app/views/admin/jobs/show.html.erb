<h1> <%= @job.title %> </h1>
<p>
  <%= simple_format(@job.description) %>
  <br><br>
  <% @resumes.each do |resume| %>
  <%= simple_format(resume.user.email) %>
  <%= simple_format(resume.content) %>
  <% end %>
</p>
